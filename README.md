# Shepherd
A context storage for LLMs to retrieve data accurately instead of grepping the code. It uses RRF algorithm to find the best result from BM25 (a standard ranking algorithm) and semantic embedding.

This project still under construction